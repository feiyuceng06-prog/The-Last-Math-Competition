import Mathlib

/-!
# Conjecture @@ID@@

@@STATEMENT@@

This file formalizes the claim established in `../main.tex`.

Conventions:
* Use Mathlib's `Nat.Prime`, `Set.range`, `Finset.card`, … . Do **not** re-define
  standard notions such as primality; a proof about a locally re-defined
  predicate proves nothing about the conjecture.
* No `sorry`, no `admit`, no new `axiom`, no `native_decide`.
-/

namespace Conjecture@@ID@@

end Conjecture@@ID@@
